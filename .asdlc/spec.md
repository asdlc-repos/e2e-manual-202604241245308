# Overview

A minimal HTTP API with a single endpoint that returns a hello world message in JSON format.

# Personas

- Developer — calls the API to verify it's running.

# Features

- A GET request to /hello returns a JSON response with a hello world message.